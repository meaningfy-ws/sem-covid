# de pus in array mare, dupa care in set,
# def test(df, target_column)
# series in python
# python equals vs is
# sorted
# python tutorial


    #     unique_elements= set()

    #     arrays = [x for x in column]

    #     for elements in arrays:
    #         for element in elements:
    #             unique_elements.add(element)
